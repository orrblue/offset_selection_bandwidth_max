% saves new xml file
function [] = make_xml_with_optimized_offsets(config_file, path_ids, weights)


% no weights bc randomized
function [] = get_xml_with_randomized_offsets(config_file, path_ids, cycle_time)


function [] = run_experiments(dir, run_times, sampling_time, duration)